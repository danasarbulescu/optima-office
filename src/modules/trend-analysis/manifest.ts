import type { ModuleManifest } from '../types';

export const manifest: ModuleManifest = {
  id: 'trend-analysis',
  name: 'Trend Analysis',
  route: 'trend-analysis',
  navLabel: 'Trend Analysis',
  navOrder: 2,
};
